// ─── Re-export mock data (ตอนต่อ API จริง เปลี่ยน mock → fetch ตรงนี้) ───

export { mockNews, mockTickers, mockStockSentiment, liveUpdate, mockTelegramNotifications, mockMarketTrends } from './mock-data';
