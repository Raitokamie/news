// ─── Re-export mock data (ตอนต่อ API จริง เปลี่ยน mock → fetch ตรงนี้) ───

export { mockNews, mockStockSentiment, liveUpdate, mockTelegramNotifications, mockMarketTrends, mockAIOutlook } from './mock-data';
